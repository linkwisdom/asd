/**
 * @file ${title} ${moduleName} Config
 * 
 * @author ${username} (${email})
 */
define(function (require, exports) {
    /**
     * 文本术语定义
     * 
     * @type {Object}
     */
    exports.text = {

    };
    
    return exports;
});