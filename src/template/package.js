/**
 * @file ${title} - 打包文件
 * - 定义pacage.js的文件目录将被视为异步加载模块
 * 
 * @author ${username} ( ${email} )
 */
define(function (require) {
    return {
        name: 'material',
        main: './main',
        subModules: []
    };
});
