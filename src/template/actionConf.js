/**
 * @file ${title}
 * 
 * @author ${username} ( ${email} )
 */
define(function (require) {
    /**
     * 当前模块配置
     * 
     * @type {Object}
     */
    var actionConf = [
        {
            path: '/${moduleId}',
            type: '${moduleId}/Action'
        }
    ];

    return actionConf;
});
